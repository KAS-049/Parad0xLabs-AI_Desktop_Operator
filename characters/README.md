Drop local `.vrm` files into this folder.

`Codex Avatar` scans this folder at startup and exposes every VRM model in the settings panel.

If the folder is empty, the app falls back to a simple animated orb so the shell still works.
